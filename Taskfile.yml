# https://taskfile.dev

version: '3'

env:
  COMPOSE_DOCKER_CLI_BUILD: 1
  DOCKER_BUILDKIT: 1

tasks:
  build:
    cmds:
      - mvnw clean
      - mvnw package

  run:
    cmds:
      - mvnw clean
      - mvnw spring-boot:run


  
